@model MonaWebApp.Models.EntityFramework.BlogCategory
@{
    ViewBag.Title = "CreateCategory";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Create Category</h2>

@*<form  method="post" >
        <div class="form-group">
            <label for="catId">Category Name</label>
            <input type="text" class="form-control" name="BlogCategoryName" id=""  placeholder="Enter Blog Category Name">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>*@

@using (Html.BeginForm("CreateCategory", "Admin", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.BlogCategoryName)
        @Html.TextBoxFor(m => m.BlogCategoryName, new { @class = "form-control" })
    </div>
    @Html.HiddenFor(m => m.BlogCategoryId)
    <button type="submit" class="btn btn-primary">Submit</button>

}

